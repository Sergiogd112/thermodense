# Frozen migration configurations

These first-party configuration snapshots remain temporarily for the migration
interface. They are not the removed root `thesis/` tree or generated artifacts,
and they will not be redesigned before issue #7.
