{
  "name": "thermodense-figure-review-tests",
  "private": true,
  "scripts": {
    "test:smoke": "playwright test --config playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
